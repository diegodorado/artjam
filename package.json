{
  "name": "artjam",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "rm build/* && parcel build src/index.html --no-source-maps --dist-dir build --public-url ."
  },
  "dependencies": {
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1",
    "dotenv-cli": "^4.1.1",
    "parcel": "^2.0.1",
    "sass": "^1.44.0"
  }
}
